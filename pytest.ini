# pytest configuration for med-z1
# Currently minimal - only housekeeping settings
# Expand when active test development begins

[pytest]
# Relocate cache to keep project root clean
cache_dir = .cache/pytest

# Look for tests in scripts/ directory (consolidated approach)
testpaths = scripts
